# ====================================
# AutoTrade Pro v3.0 - 필수 패키지
# ====================================

# 핵심 패키지
requests>=2.31.0
urllib3>=2.0.0

# 데이터 처리 (Windows 호환 버전 - pre-built wheels 사용)
pandas>=2.2.0
numpy>=1.26.0,<2.0.0

# 웹 프레임워크 (대시보드)
Flask>=3.0.0
Werkzeug>=3.0.0
flask-socketio>=5.3.0
python-socketio>=5.10.0

# WebSocket
websocket-client>=1.7.0

# AI 분석
google-generativeai>=0.3.0

# 환경변수 관리
python-dotenv>=1.0.0

# 설정 관리
PyYAML>=6.0.0

# 로깅
colorlog>=6.8.0
loguru>=0.7.0

# 데이터베이스
SQLAlchemy>=2.0.0

# 유틸리티
pytz>=2023.3
python-dateutil>=2.8.0

# ====================================
# v3.0 신규 패키지
# ====================================

# CORS 지원
flask-cors>=4.0.0

# 비동기 HTTP (성능 최적화)
httpx>=0.25.0
aiohttp>=3.9.0

# 캐싱 (Redis)
redis>=5.0.0
hiredis>=2.2.0  # Redis 성능 향상

# 백그라운드 작업 큐
rq>=1.15.0

# 데이터 직렬화
msgpack>=1.0.7

# 기계학습 (ML 예측)
scikit-learn>=1.3.0
xgboost>=2.0.0

# ====================================
# 선택적 패키지 (설치 실패 시 주석 처리)
# ====================================

# 기술적 분석 (ta 패키지 대신 pandas-ta 사용 권장)
# ta>=0.11.0

# DB 마이그레이션 (고급 기능)
# alembic>=1.13.0

# Deep Learning (LSTM 예측)
# tensorflow>=2.15.0
# torch>=2.1.0
